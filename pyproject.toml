[project]
name = "contractiq"
version = "0.1.0"
description = "RAG system for legal contract Q&A, benchmarked on CUAD"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py311"
